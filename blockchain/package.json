{
  "name": "fintastech-onchain",
  "version": "1.0.0",
  "description": "FintasTech on-chain research vault and signal registry — EDUCATIONAL paper-trading. Mock-only assets. NOT investment advice.",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "fund:local": "hardhat run scripts/fund-local.js --network localhost",
    "seed:local": "hardhat run scripts/seed-demo.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5"
  }
}
